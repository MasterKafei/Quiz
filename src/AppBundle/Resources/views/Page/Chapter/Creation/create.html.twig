{% extends '@Layout/Page/Admin/base.html.twig' %}

{% block sub_title %}{{ 'tab_sub_title.chapters'|trans({}, 'label') }}{% endblock sub_title %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">{{ 'contribution.chapter.creation.title'|trans({}, 'messages') }}</h1>
            <span class="text-muted">{{ 'contribution.chapter.creation.sub_title'|trans({}, 'messages') }}</span>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12 col-md-12">
            {{ form(form) }}
        </div>
    </div>
{% endblock body %}